<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.nolan.learnenglishwords">
    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:theme="@style/AppTheme">
        <activity android:name=".activities.CreateDictionaryActivity"/>
        <activity android:name=".activities.CreateFirstDictionaryActivity"
            android:noHistory="true"/>
        <activity android:name=".activities.AddCardActivity"/>
        <activity android:name=".activities.TrainingActivity"/>
        <activity android:name=".activities.LauncherActivity"
            android:noHistory="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
